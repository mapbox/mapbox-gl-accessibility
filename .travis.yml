language: node_js

node_js: 10

os: osx

cache:
  directories:
    - node_modules

before_install:
  - echo $MAPBOX_ACCESS_TOKEN | cut -f 2 -d . | base64 --decode

install: npm ci

script: npm run lint
